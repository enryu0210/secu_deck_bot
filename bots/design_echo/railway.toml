# =====================================================================
# Design Echo — Railway 서비스 설정
# =====================================================================

[build]
builder = "DOCKERFILE"
dockerfilePath = "bots/design_echo/Dockerfile"

[deploy]
startCommand = "uv run python -m design_echo.main"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
