# =====================================================================
# Code Sentinel — Railway 서비스 설정
# =====================================================================

[build]
builder = "DOCKERFILE"
dockerfilePath = "bots/code_sentinel/Dockerfile"

[deploy]
startCommand = "uv run python -m code_sentinel.main"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
